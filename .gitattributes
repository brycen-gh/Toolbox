# To prevent CRLF line endings in shell scripts, we can use the following .gitattributes configuration. 
#This ensures that all shell scripts are treated as text files and have LF line endings, which is important for compatibility across different operating systems.

run-once/run-once text eol=lf
*.sh text eol=lf
